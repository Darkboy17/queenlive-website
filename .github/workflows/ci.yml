name: Build, Push & Deploy Docker Image

on:
  push:
    branches:
      - main
    paths:
      - 'public/**'
      - 'js/**'
      - 'css/**'
      - 'components/**'
      - 'index.html'
      - 'build.js'
      - 'package.json'
      - 'Dockerfile'
      - '.github/workflows/ci.yml'
  pull_request:
    branches:
      - main

jobs:
  build-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      # -----------------------------------------------------------
      # STEP 1️⃣: Detect if public/ changed (controls npm run build)
      # -----------------------------------------------------------
      - name: Detect changes in public folder
        id: detect_public
        run: |
          echo "🔍 Checking if 'public/' changed..."
          if git rev-parse HEAD^ >/dev/null 2>&1; then
            if git diff --quiet HEAD^ HEAD -- public/; then
              echo "🟢 No changes in public/"
              echo "PUBLIC_CHANGED=false" >> $GITHUB_ENV
            else
              echo "🟡 Changes detected in public/"
              echo "PUBLIC_CHANGED=true" >> $GITHUB_ENV
            fi
          else
            echo "First commit — forcing build"
            echo "PUBLIC_CHANGED=true" >> $GITHUB_ENV
          fi

      # -----------------------------------------------------------
      # STEP 2️⃣: Detect if any relevant project files changed
      # -----------------------------------------------------------
      - name: Detect relevant project changes
        id: detect_relevant
        run: |
          echo "🔍 Checking if relevant code/config files changed..."
          if git rev-parse HEAD^ >/dev/null 2>&1; then
            PATHS="js/ css/ components/ index.html build.js package.json Dockerfile .github/workflows/ci.yml"
            if git diff --quiet HEAD^ HEAD -- $PATHS; then
              echo "🟢 No relevant changes in app code/config"
              echo "RELEVANT_CHANGED=false" >> $GITHUB_ENV
            else
              echo "🟡 Relevant changes detected"
              echo "RELEVANT_CHANGED=true" >> $GITHUB_ENV
            fi
          else
            echo "First commit — forcing deploy"
            echo "RELEVANT_CHANGED=true" >> $GITHUB_ENV
          fi

      # -----------------------------------------------------------
      # STEP 4️⃣: Conditional build
      # -----------------------------------------------------------
      - name: Run build.js (conditionally)
        if: env.PUBLIC_CHANGED == 'true' || env.RELEVANT_CHANGED == 'true'
        run: |
          echo "🛠️ Running npm run build because relevant/public files changed"
          npm run build

      - name: Skip npm run build
        if: env.PUBLIC_CHANGED != 'true' && env.RELEVANT_CHANGED != 'true'
        run: |
          echo "⏭️ Skipping npm run build (no relevant/public changes)"
          mkdir -p dist

      # -----------------------------------------------------------
      # STEP 5️⃣: Build and push Docker image if app changed
      # -----------------------------------------------------------
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        if: env.RELEVANT_CHANGED == 'true' || env.PUBLIC_CHANGED == 'true'
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/queenlive:latest
          build-args: |
            PUBLIC_CHANGED=${{ env.PUBLIC_CHANGED }}

      - name: Skip Docker build
        if: env.RELEVANT_CHANGED != 'true' && env.PUBLIC_CHANGED != 'true'
        run: echo "⏭️ Skipping Docker build (no relevant changes)"

      - name: Logout from Docker Hub
        run: docker logout

      # -----------------------------------------------------------
      # STEP 6️⃣: Deploy only if relevant or public changed
      # -----------------------------------------------------------
      - name: Deploy to GCP VM via SSH
        if: env.RELEVANT_CHANGED == 'true' || env.PUBLIC_CHANGED == 'true'
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.GCP_VM_HOST }}
          username: ${{ secrets.GCP_VM_USER }}
          key: ${{ secrets.GCP_VM_SSH_KEY }}
          script: |
            echo "🚀 Pulling latest Docker image and redeploying..."
            docker pull ${{ secrets.DOCKERHUB_USERNAME }}/queenlive:latest
            docker stop queenlive || true
            docker rm queenlive || true
            docker run -d --name queenlive -p 80:80 ${{ secrets.DOCKERHUB_USERNAME }}/queenlive:latest

      - name: Skip deploy
        if: env.RELEVANT_CHANGED != 'true' && env.PUBLIC_CHANGED != 'true'
        run: echo "⏭️ Skipping deploy — nothing new to release"